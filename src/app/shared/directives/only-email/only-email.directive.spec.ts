import { OnlyEmailDirective } from './only-email.directive';

describe('OnlyEmailDirective', () => {
  it('should create an instance', () => {
    const directive = new OnlyEmailDirective();
    expect(directive).toBeTruthy();
  });
});
